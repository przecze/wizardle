services:
  backend:
    build: ./backend
    volumes:
      - ./backend:/app
      - ./preprocessing:/preprocessing:ro
      - ./fragment_context_cache:/fragment_context_cache
    secrets:
      - openrouter_key
    environment:
      - PYTHONUNBUFFERED=1
      # Wedding special puzzle date; defaults to 2026-07-25 if unset (see
      # backend/main.py). Override locally to preview early, e.g.:
      #   WEDDING_DATE=$(date +%F) docker compose up -d backend
      - WEDDING_DATE=${WEDDING_DATE:-}
    networks:
      - localnet
    command: uvicorn main:app --reload --workers 1 --host 0.0.0.0 --port 8000
    restart: always

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
      target: development
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/public:/app/public
      - ./frontend/index.html:/app/index.html
      - ./frontend/vite.config.js:/app/vite.config.js
      - ./frontend/tsconfig.json:/app/tsconfig.json
      - ./frontend/package.json:/app/package.json
      - /app/node_modules
    environment:
      - NODE_ENV=development
      # See WEDDING_DATE comment on the backend service above.
      - VITE_WEDDING_DATE=${WEDDING_DATE:-}
    networks:
      - localnet
    restart: unless-stopped

  nginx:
    image: nginx:1.31.0-alpine
    volumes:
      - ./nginx.conf.template:/etc/nginx/templates/nginx.conf.template:ro
      - ./nginx/html:/usr/share/nginx/html:ro
    environment:
      DEV_MODE: "true"
      NGINX_ENVSUBST_FILTER: DEV_MODE
      NGINX_ENVSUBST_OUTPUT_DIR: /etc/nginx
    networks:
      - localnet
    depends_on:
      - backend
      - frontend
    ports:
      - 127.0.0.1:8090:80
    restart: unless-stopped

  # Production: self-contained nginx with built frontend
  site:
    build:
      context: .
      dockerfile: Dockerfile.frontend
      target: production
    networks:
      - localnet
      - nginx-proxy
    environment:
      VIRTUAL_HOST: wizardle.net
      LETSENCRYPT_HOST: wizardle.net
      DEV_MODE: "false"
      NGINX_ENVSUBST_FILTER: DEV_MODE
      NGINX_ENVSUBST_OUTPUT_DIR: /etc/nginx
    depends_on:
      - backend
    restart: always
    profiles:
      - production

  # Frontend tests: docker compose --profile test run --rm tester
  tester:
    build:
      context: .
      dockerfile: Dockerfile.frontend
      target: test
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/vitest.config.ts:/app/vitest.config.ts
      - /app/node_modules
    profiles:
      - test

secrets:
  openrouter_key:
    file: ./openrouter_prod_api_key.txt

networks:
  localnet:
  nginx-proxy:
    external: true
