ensure no dead code
no duplicated code
FE and BE only send around stuff that the other actually needs
FE and BE don't duplicate logic (they send ALL the stuff the other needs)
everything is named appropriately
no misleading comments
file structure is appropriate
make sure frontend is implemented without incline scritps or styles to allow the CSP in nginx config
make sure best practices for react apps are followed in frontend